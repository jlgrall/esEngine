
var 
	// Prototype for all eLinks.
	// eLinks, unlike cLinks, must be defined in each es,
	// because an entity is just a number and thus it cannot
	// tell to which es it belongs.
	eLinkProto = {};
